{
  "gateway": {
    "mode": "local",
    "port": 18789,
    "bind": "auto",
    "auth": {
      "mode": "token",
      "token": "${OPENCLAW_GATEWAY_TOKEN}"
    },
    "controlUi": {
      "enabled": true,
      "allowedOrigins": [
        "http://127.0.0.1:18789",
        "http://localhost:18789"
      ]
    }
  },
  "agents": {
    "defaults": {
      "workspace": "/app",
      "model": {
        "primary": "primary_provider/${PRIMARY_MODEL}",
        "fallbacks": []
      },
      "models": {
        "primary_provider/${PRIMARY_MODEL}": {
          "alias": "primary"
        },
        "vision_provider/${VISION_MODEL}": {
          "alias": "vision"
        }
      }
    }
  },
  "models": {
    "mode": "merge",
    "providers": {
      "primary_provider": {
        "baseUrl": "${PRIMARY_PROVIDER_BASE_URL}",
        "apiKey": "${PRIMARY_PROVIDER_API_KEY}",
        "api": "openai-completions",
        "models": [
          {
            "id": "${PRIMARY_MODEL}",
            "name": "Primary Model",
            "input": ["text"]
          }
        ]
      },
      "vision_provider": {
        "baseUrl": "${VISION_PROVIDER_BASE_URL}",
        "apiKey": "${VISION_PROVIDER_API_KEY}",
        "api": "openai-completions",
        "models": [
          {
            "id": "${VISION_MODEL}",
            "name": "Vision Model",
            "input": ["text", "image"]
          }
        ]
      }
    }
  }
}
